{
  "name": "clash-of-brains",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "server": "nodemon backend/server.js",
    "client": "serve -s frontend/build",
    "build": "npm run build --prefix frontend",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
