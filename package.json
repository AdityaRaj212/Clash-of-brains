{
  "name": "clash-of-brains",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build-all": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "concurrently \"npm start --prefix backend\" \"serve -s frontend/build\""
  },
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
